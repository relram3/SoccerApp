import React, { Component } from "react";

class Navbar extends Component {
  render() {
    return (
      <header className="header">
        <span className="title">Ramzi</span>
        <button className="nav-btn" />
      </header>
    );
  }
}

export default Navbar;
